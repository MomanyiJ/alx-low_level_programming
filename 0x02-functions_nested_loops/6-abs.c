#include "main.h"

/**
 * _abs - Calculates absolute value
 *
 * Return: Absolute value
 * @n: takes an int value
 */
int _abs(int n)
{
	if (n > 0)
		return (n);
	else
		return (n * -1);
	return (n);

}
